@model QuizApplication.Models.Subject.SubjectViewModel
@{
    ViewData["Title"] = "Subject Detail";
}

<div class="body">

    @if (Model is not null)
    {
        <dl class="row">
            <dt class="col-sm-4">
                Subject Name:
            </dt>

            <dd class="col-sm-8">
                @Model.Name
            </dd>

            <dt class="col-sm-4">
                Description:
            </dt>

            <dd class="col-sm-8">
                @Model.Description
            </dd>
        </dl>
        <br />
        <div class="btn-group">
            <a asp-controller="Subject" asp-action="Update" asp-route-id="@Model.Id" class="btn btn-success">Update</a>
            &nbsp;
            <form asp-action="DeleteSubject" asp-route-id="@Model.Id">
                <button type="submit" class="btn btn-danger">Delete</button>
            </form>
            &nbsp;
            <a asp-controller="Subject" asp-action="Index" class="btn btn-outline-secondary">Back</a>
        </div>
    }
</div>
